#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int n, s;
    cin >> n;
    s = sqrt(n);

    cout << "The largest square has side length "<< s << "." << "\n";
    return 0;
}